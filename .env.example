DATABASE_URL=jdbc:mysql://localhost:3306/cleanmediasapi
DATABASE_USERNAME=cleanmediasapi
DATABASE_PASSWORD=cleanmediasapi

# Gerar com: openssl genrsa -out private.pem 2048 && openssl rsa -in private.pem -pubout -out public.pem
# Base64 puro (sem headers PEM): base64 -w 0 private.pem
JWT_PUBLIC_KEY_CONTENT=
JWT_PRIVATE_KEY_CONTENT=

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
